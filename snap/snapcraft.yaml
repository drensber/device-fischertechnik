name: ft-device-service # you probably want to 'snapcraft register <name>'
# base: core16 # the base snap is the execution environment for this snap
version: '0.1' # just for humans, typically '1.2+git' or '1.3.2'
summary: Single-line elevator pitch for your amazing snap # 79 char long summary
description: |
  Fischertechnic robot device service for EdgeX with support for robot
  automation.
grade: devel # must be 'stable' to release into candidate/stable channels
confinement: devmode # use 'strict' once you have the right plugs and slots



parts:
  ft-device-service:
    #source: http://davidr@lm-gitlab.beechwoods.com/edgex/EdgeXfoundry/device-fischertechnik.git
    source: .
    source-type: local
    #source-branch: robot_automation_demo 
    plugin: maven

